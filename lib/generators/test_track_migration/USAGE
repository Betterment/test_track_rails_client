Description:
    Generates a migration to update test track configuation

Example:
    rails generate test_track_migration AddSplitName

    This will create:
        db/migrate/current_time_add_split_name.rb

Note:
    rails generate test_track_migration RetireSplitName
    OR
    rails generate test_track_migration FinishSplitName

    will generate a migration to finish the named split
